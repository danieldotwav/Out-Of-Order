package src;

public interface Constants {
    char PADDING_CHAR_TITLE = '~';   // Padding character for the title
    char PADDING_CHAR_MENU = '-';    // Padding character for the menu
    char PADDING_CHAR_SELECTION = '*';  // Padding character for the battle
    int TOTAL_WIDTH = 75;       // Total width of the console window
    int MAX_NUM = 100;

    int EASY_REWARD_PTS = 1;
    int MEDIUM_REWARD_PTS = 2;
    int HARD_REWARD_PTS = 3;

    int EASY_NUM_ITEMS = 5;
    int MEDIUM_NUM_ITEMS = 10;
    int HARD_NUM_ITEMS = 15;
    int TWO = 2;
}

